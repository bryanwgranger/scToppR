% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toppFun.R
\name{addToppData}
\alias{addToppData}
\title{Add toppData results to SingleCellExperiment or SummarizedExperiment metadata}
\usage{
addToppData(
  sce,
  toppData_results,
  slot_name = "toppData",
  include_params = TRUE
)
}
\arguments{
\item{sce}{A SingleCellExperiment or SummarizedExperiment object}

\item{toppData_results}{A data.frame of toppData results from toppFun()}

\item{slot_name}{Name for the metadata slot (default: "toppData")}

\item{include_params}{Logical, whether to include analysis parameters and timestamp
in a separate \code{slot_name_params} slot (default: TRUE)}
}
\value{
SingleCellExperiment or SummarizedExperiment object with toppData stored in metadata
}
\description{
A convenience function to store toppData enrichment results in the metadata
slot of a SingleCellExperiment or SummarizedExperiment object. Results are
stored directly under the specified slot name, with optional analysis parameters
stored in a separate \code{slot_name_params} slot.
}
\examples{
library(airway)
data("airway")  # example SummarizedExperiment object
data("toppdata.airway")  # example toppData results
se_with_topp <- addToppData(airway, toppdata.airway)

# Access results directly
topp_results <- S4Vectors::metadata(se_with_topp)$toppData

# Access analysis parameters (if include_params = TRUE)
topp_params <- S4Vectors::metadata(se_with_topp)$toppData_params
}
